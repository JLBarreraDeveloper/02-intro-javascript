const firstName = 'Jhon';
const lastName = 'Doe';

const fullName = `${firstName} ${lastName}`;

function getSaludo(name) {
  return `Hola ${name}`;
}

console.log(getSaludo(fullName)); // Output: Hola John`)